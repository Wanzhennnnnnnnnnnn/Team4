<!DOCTYPE html>
<html>

<head>
    <title>BuildLink - 密碼重設</title>
    <style>
        /* 繼承自 login.hjs 的主要樣式，以維持一致性 */
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: linear-gradient(135deg, #f0f4f8 0%, #d9e2ec 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #333;
        }

        .login-container {
            background-color: #fff;
            padding: 35px 40px;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(0, 0, 0, 0.05);
            width: 350px;
            border: 1px solid #e0e6ed;
            text-align: center;
        }

        h1 {
            text-align: center;
            color: #000;
            margin-bottom: 20px;
            font-size: 2em;
            font-weight: 800;
        }

        .form-group {
            margin-bottom: 12px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 700;
            color: #1a1a1a;
            font-size: 1em;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 10px 15px;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            box-sizing: border-box;
            transition: border-color 0.3s, box-shadow 0.3s;
            background-color: #fff;
        }

        .error-message,
        .success-message {
            padding: 8px 10px;
            border-radius: 6px;
            text-align: center;
            margin-bottom: 15px;
        }

        .error-message {
            color: #991b1b;
            background-color: #fee2e2;
            border: 1px solid #fca5a5;
        }

        .success-message {
            color: #065f46;
            background-color: #d1fae5;
            border: 1px solid #a7f3d0;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #f59e0b; /* 重設使用橙色 */
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: bold;
            margin-top: 15px;
            transition: background-color 0.3s, transform 0.1s;
            box-shadow: 0 4px 6px rgba(245, 158, 11, 0.3);
        }

        button:hover {
            background-color: #d97706;
            box-shadow: 0 6px 8px rgba(245, 158, 11, 0.4);
        }

        button:active {
            transform: translateY(1px);
        }
        
        .return-link {
            text-align: center;
            font-size: 0.9em;
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #e5e7eb;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <h1>🔐 密碼重設</h1>
        <p style="margin-bottom: 20px; color: #555;">請輸入您的帳號和新的密碼。</p>

        <!-- 訊息顯示 -->
        {{#success}}
        <p class="success-message">{{success}}</p>
        {{/success}}

        {{#error}}
        <p class="error-message">錯誤：{{error}}</p>
        {{/error}}

        <form action="/forgot-password" method="POST">

            <!-- 身分選擇下拉選單 -->
            <div class="form-group">
                <label for="role">您的身分</label>
                <select id="role" name="role" required>
                    <option value="" disabled selected>請選擇您的身分</option>
                    <option value="員工">員工</option>
                    <option value="Contractor">承包商</option>
                    <option value="Supplier">供應商</option>
                </select>
            </div>

            <div class="form-group">
                <label for="username">帳號 (員工編號/夥伴帳號)</label>
                <input type="text" id="username" name="username" required>
            </div>
            
            <div class="form-group">
                <label for="new_password">新密碼</label>
                <input type="password" id="new_password" name="new_password" required>
            </div>
            
            <button type="submit">確認重設密碼</button>
        </form>

        <p class="return-link">
            <a href="/login">▶ 返回登入頁面</a>
        </p>
    </div>
</body>

</html>