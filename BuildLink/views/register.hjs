<!DOCTYPE html>
<html>
<head>
    <title>BuildLink - Join Us</title>
    <style>
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: linear-gradient(135deg, #FDFBFB 0%, #F9F3F3 100%); /* 主題背景 */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px 0;
            margin: 0;
            color: #333;
        }

        .login-container {
            background-color: #fff;
            padding: 35px 40px;
            border-radius: 16px;
            box-shadow: 0 8px 25px rgba(212, 165, 165, 0.15), 0 0 0 1px rgba(212, 165, 165, 0.1);
            width: 400px;
            border: 1px solid #F5E6E8;
            text-align: center;
        }

        h1 {
            text-align: center;
            color: #2D3748;
            margin-bottom: 10px;
            font-size: 1.8em;
            font-weight: 800;
        }
        
        .subtitle {
            color: #718096;
            margin-bottom: 30px;
            font-size: 0.95em;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #4a5568;
            font-weight: 600;
            font-size: 0.9em;
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            border: 1px solid #cbd5e0;
            border-radius: 10px;
            box-sizing: border-box;
            font-size: 1em;
            transition: all 0.2s;
        }

        .form-group input:focus {
            outline: none;
            border-color: #D4A5A5; /* 主題色 */
            box-shadow: 0 0 0 3px rgba(212, 165, 165, 0.2);
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #D4A5A5; /* 主題色 */
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1em;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s, transform 0.1s;
            margin-top: 10px;
            box-shadow: 0 4px 6px rgba(212, 165, 165, 0.3);
        }

        button:hover {
            background-color: #C9A0A0;
            transform: translateY(-1px);
        }
        
        .error-message {
            background-color: #FFF5F5;
            color: #C53030;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.9em;
            text-align: left;
            border: 1px solid #FEB2B2;
        }

        .register-link {
            text-align: center;
            font-size: 0.9em;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #F5E6E8;
            color: #666;
        }

        .register-link a {
            color: #D4A5A5; /* 主題色 */
            text-decoration: none;
            font-weight: 600;
            margin-left: 5px;
        }
        
        .register-link a:hover {
            text-decoration: underline;
            color: #C9A0A0;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <!-- Icon -->
        {{!-- <div style="display: flex; justify-content: center; margin-bottom: 15px;">
            <div style="width: 48px; height: 48px; background-color: #FFF5F5; border-radius: 12px; display: flex; align-items: center; justify-content: center;">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#D4A5A5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="8.5" cy="7" r="4"></circle><line x1="20" y1="8" x2="20" y2="14"></line><line x1="23" y1="11" x2="17" y2="11"></line></svg>
            </div>
        </div> --}}

        <h1>Join BuildLink</h1>
        <p class="subtitle">Start sourcing materials and managing projects.</p>

        {{#error}}
        <p class="error-message">{{error}}</p>
        {{/error}}

        <form action="/register" method="POST">
            
            <div class="form-group">
                <label for="name">Company Name / Full Name</label>
                <input type="text" id="name" name="name" required placeholder="e.g., Grand Construction Co.">
            </div>

            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" required placeholder="This will be your login ID">
            </div>

            <div class="form-group">
                <label for="phone">Phone Number</label>
                <input type="text" id="phone" name="phone" placeholder="e.g., 0912-345-678">
            </div>
            
            <div class="form-group">
                <label for="address">Company Address</label>
                <input type="text" id="address" name="address" placeholder="e.g., 101 Xinyi Road, Taipei...">
            </div>

            <div class="form-group">
                <label for="password">Set Password</label>
                <input type="password" id="password" name="password" required placeholder="Create a secure password">
            </div>

            <button type="submit">Create Account</button>
        </form>

        <div class="register-link">
            Already have an account? <a href="/login">Log In</a>
        </div>
    </div>
</body>
</html>