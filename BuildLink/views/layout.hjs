<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} | BuildLink</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 text-slate-900">

    <div class="flex h-screen overflow-hidden">
        
        <!-- Sidebar -->
        <aside class="w-64 bg-slate-900 text-slate-300 flex flex-col hidden md:flex">
            <div class="p-6 border-b border-slate-800">
                <div class="flex items-center gap-2 text-white mb-1">
                    <div class="bg-blue-600 p-1.5 rounded-lg">
                        <i data-lucide="building-2" class="w-5 h-5"></i>
                    </div>
                    <span class="text-xl font-bold">BuildLink</span>
                </div>
                <p class="text-xs text-slate-500 mt-2">Contractor Portal</p>
            </div>

            <nav class="flex-1 p-4 space-y-1">
                <a href="/contractor/dashboard" class="flex items-center gap-3 px-4 py-3 rounded-lg transition-colors {{#isDashboard}}bg-blue-600 text-white{{/isDashboard}} {{^isDashboard}}hover:bg-slate-800 hover:text-white{{/isDashboard}}">
                    <i data-lucide="layout-dashboard" class="w-5 h-5"></i>
                    <span class="font-medium">Dashboard</span>
                </a>
                <a href="/contractor/projects" class="flex items-center gap-3 px-4 py-3 rounded-lg transition-colors {{#isProjects}}bg-blue-600 text-white{{/isProjects}} {{^isProjects}}hover:bg-slate-800 hover:text-white{{/isProjects}}">
                    <i data-lucide="clipboard-list" class="w-5 h-5"></i>
                    <span class="font-medium">Projects</span>
                </a>
                <a href="/contractor/suppliers" class="flex items-center gap-3 px-4 py-3 rounded-lg transition-colors {{#isSuppliers}}bg-blue-600 text-white{{/isSuppliers}} {{^isSuppliers}}hover:bg-slate-800 hover:text-white{{/isSuppliers}}">
                    <i data-lucide="users" class="w-5 h-5"></i>
                    <span class="font-medium">Suppliers</span>
                </a>
                <a href="/contractor/orders" class="flex items-center gap-3 px-4 py-3 rounded-lg transition-colors {{#isOrders}}bg-blue-600 text-white{{/isOrders}} {{^isOrders}}hover:bg-slate-800 hover:text-white{{/isOrders}}">
                    <i data-lucide="package" class="w-5 h-5"></i>
                    <span class="font-medium">Material Orders</span>
                </a>
            </nav>

            <div class="p-4 border-t border-slate-800">
                <a href="/logout" class="flex items-center gap-3 px-4 py-2 text-slate-400 hover:text-white transition-colors">
                    <i data-lucide="log-out" class="w-5 h-5"></i>
                    <span>Sign Out</span>
                </a>
            </div>
        </aside>

        <!-- Main Content Wrapper -->
        <div class="flex-1 flex flex-col h-screen overflow-hidden">
            
            <!-- Navbar -->
            <header class="bg-white border-b border-slate-200 h-16 flex items-center justify-between px-8 sticky top-0 z-20">
                <div class="flex items-center gap-4 md:hidden">
                    <span class="font-bold text-lg">BuildLink</span>
                </div>
                <div class="hidden md:block text-sm breadcrumbs text-slate-500">
                    <!-- Breadcrumbs placeholder -->
                    Portal / {{title}}
                </div>
                
                <div class="flex items-center gap-4">
                    <div class="text-right hidden sm:block">
                        <div class="text-sm font-medium text-slate-800">{{contractorName}}</div>
                        <div class="text-xs text-slate-500">Project Manager</div>
                    </div>
                    <div class="h-10 w-10 bg-slate-100 rounded-full flex items-center justify-center border border-slate-200 text-slate-500">
                        <i data-lucide="user" class="w-5 h-5"></i>
                    </div>
                </div>
            </header>

            <!-- Content Area -->
            <main class="flex-1 overflow-auto p-8 bg-slate-50">
                <div class="max-w-7xl mx-auto">
                    {{{ content }}}
                </div>
            </main>
        </div>
    </div>

    <script>
        // Initialize Icons
        lucide.createIcons();
    </script>
</body>
</html>