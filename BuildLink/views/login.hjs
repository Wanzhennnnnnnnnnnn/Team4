<!DOCTYPE html>
<html>

<head>
    <title>BuildLink - Contractor Login</title>
    <style>
        /* --- Global & Background --- */
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: linear-gradient(135deg, #FDFBFB 0%, #F9F3F3 100%); /* 背景改為與 Layout 一致的暖色調 */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #333;
        }

        .login-container {
            background-color: #fff;
            padding: 35px 40px;
            border-radius: 16px; /* 圓角加大一點，符合整體風格 */
            box-shadow: 0 8px 25px rgba(212, 165, 165, 0.15), 0 0 0 1px rgba(212, 165, 165, 0.1); /* 陰影帶一點主題色 */
            width: 350px;
            border: 1px solid #F5E6E8; /* 邊框改淡粉色 */
            text-align: center;
        }

        h1 {
            text-align: center;
            color: #2D3748;
            margin-bottom: 10px;
            font-size: 1.8em;
            font-weight: 800;
        }
        
        .subtitle {
            color: #718096;
            margin-bottom: 30px;
            font-size: 0.95em;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #4a5568;
            font-weight: 600;
            font-size: 0.9em;
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            border: 1px solid #cbd5e0;
            border-radius: 10px; /* 圓角調整 */
            box-sizing: border-box;
            font-size: 1em;
            transition: all 0.2s;
        }

        .form-group input:focus {
            outline: none;
            border-color: #D4A5A5; /* 主題色 */
            box-shadow: 0 0 0 3px rgba(212, 165, 165, 0.2); /* 主題色光暈 */
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #D4A5A5; /* 主題色 */
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1em;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s, transform 0.1s;
            margin-top: 10px;
            box-shadow: 0 4px 6px rgba(212, 165, 165, 0.3);
        }

        button:hover {
            background-color: #C9A0A0; /* 深一點的主題色 */
            transform: translateY(-1px);
        }
        
        button:active {
            transform: translateY(0);
        }
        
        .error-message {
            background-color: #FFF5F5;
            color: #C53030;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.9em;
            text-align: left;
            border: 1px solid #FEB2B2;
        }
        
        .success-message {
            background-color: #F0FFF4;
            color: #276749;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.9em;
            text-align: left;
            border: 1px solid #9AE6B4;
        }

        .register-link {
            text-align: center;
            font-size: 0.9em;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #F5E6E8;
            display: flex;
            justify-content: space-between; 
        }

        .register-link a {
            color: #D4A5A5; /* 主題色 */
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s;
        }
        
        .register-link a:hover {
            text-decoration: underline;
            color: #C9A0A0;
        }
        
        .forgot-link {
            color: #A0AEC0;
            text-decoration: none;
            font-size: 0.9em;
            font-weight: normal !important;
        }
        
        .forgot-link:hover {
            text-decoration: underline;
            color: #718096;
        }
    </style>
</head>

<body>
    <div class="login-container">
        {{!-- <!-- 加入 Logo Icon -->
        <div style="display: flex; justify-content: center; margin-bottom: 15px;">
            <div style="width: 48px; height: 48px; background-color: #FFF5F5; border-radius: 12px; display: flex; align-items: center; justify-content: center;">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#D4A5A5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2" ry="2"></rect><path d="M9 22v-4h6v4"></path><path d="M8 6h.01"></path><path d="M16 6h.01"></path><path d="M12 6h.01"></path><path d="M12 10h.01"></path><path d="M12 14h.01"></path><path d="M16 10h.01"></path><path d="M16 14h.01"></path><path d="M8 10h.01"></path><path d="M8 14h.01"></path></svg>
            </div>
        </div> --}}
        
        <h1>BuildLink</h1>
        <p class="subtitle">Contractor Procurement Portal</p>

        {{#success}}
        <p class="success-message">{{success}}</p>
        {{/success}}

        {{#error}}
        <p class="error-message">{{error}}</p>
        {{/error}}

        <form action="/login" method="POST">
            
            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" required placeholder="Enter your email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required placeholder="Enter your password">
            </div>

            <button type="submit">Sign In</button>
        </form>

        <div class="register-link">
            <a href="/register">Create Account</a>
            <a href="/forgot-password" class="forgot-link">Forgot Password?</a>
        </div>
    </div>
</body>

</html>